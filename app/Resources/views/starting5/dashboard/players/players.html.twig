{% for player in myPlayers %}
    <div class="col-md-4 my-players wow bounceIn">
        {% include 'starting5/players/player.html.twig' with {'player': player} %}
        <button class="btn btn-danger discard discard-{{ player.id }}">Discard</button>
    </div>
    <script type="text/javascript">
        $(".discard-{{ player.id }}").click(function () {
            $.ajax({
                url: '{{ path('discard.player') }}',
                type: 'POST',
                data: 'userPlayerId=' + {{ player.id }} + '&page='+ currentPage,
                beforeSend: function () {
                    loading.show();
                },
                complete: function () {
                    loading.hide();
                },
                success: function (success) {
                    $('.myPlayers').empty().append(success);
                }
            });
        });
    </script>
{% endfor %}