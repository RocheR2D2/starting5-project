<div class="wow fadeInUpBig" style="margin:150px;">
    <div class="row text-center">
        {% for player in packContent %}
            <div class="col-md-4">
                {% include 'starting5/players/player.html.twig' with {'player': player.player} %}
            </div>
        {% endfor %}
        <div class="col-md-6 text-center">
            {{ trainerStadium.trainer.firstname }}
            {{ trainerStadium.trainer.lastname }}
        </div>
        <div class="col-md-6 text-center">
            {{ trainerStadium.stadium.name }}
        </div>
    </div>
</div>